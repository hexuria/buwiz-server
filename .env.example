# Auth stack runtime mode.
AUTH_TRANSPORT=both
AUTH_PRODUCTION_MODE=false
AUTH_REQUIRE_TRUSTED_INGRESS=false
AUTH_TRUSTED_INGRESS_KEY_BASE64=
AUTH_TRUSTED_INGRESS_AUDIENCE=buwiz-server
AUTH_TRUSTED_INGRESS_MAX_AGE_SECONDS=5
# Prefer `make spin listen=HOST:PORT` so public origin values derive together.
# If you set AUTH_PUBLIC_BASE_URL here, keep it aligned with listen / browser URL.
AUTH_INGRESS_LISTEN=127.0.0.1:3008
AUTH_INGRESS_BACKEND_ORIGIN=http://127.0.0.1:3009
AUTH_INGRESS_POSTGRES_POOL_SIZE=16
AUTH_INGRESS_TOKEN_CACHE_CAPACITY=4096
AUTH_INGRESS_CACHE_REVALIDATE_MS=1000

# Default authentication path.
AUTH_ENABLE_PASSWORD_LOGIN=true
AUTH_PASSWORD_MIN_LENGTH=15
AUTH_PASSWORD_KDF=argon2id
AUTH_PASSWORD_ARGON2_MEMORY_KIB=19456
AUTH_PASSWORD_ARGON2_ITERATIONS=2
AUTH_PASSWORD_ARGON2_PARALLELISM=1
AUTH_PASSWORD_PBKDF2_ITERATIONS=600000
# Leave AUTH_PUBLIC_BASE_URL / AUTH_JWT_ISSUER unset to derive from Makefile `listen`.
# AUTH_PUBLIC_BASE_URL=http://127.0.0.1:3008
# AUTH_JWT_ISSUER=http://127.0.0.1:3008
AUTH_JWT_AUDIENCE=buwiz-server
AUTH_JWT_KID=buwiz-server-dev-hs256
AUTH_JWT_SECRET=
AUTH_JWT_ALGORITHM=HS256
AUTH_JWT_PRIVATE_KEY_DER_BASE64=
AUTH_JWT_PUBLIC_JWKS_JSON=
AUTH_JWT_KEY_RING_JSON=
AUTH_BOOTSTRAP_ADMIN_EMAILS=
# Per-project root secret. `ddd init` writes 32 random bytes here; the app
# refuses to start without it. Every sealing key, pepper, and MAC below is
# HKDF-SHA256 derived from it unless you set the specific variable.
# Generate with: head -c 32 /dev/urandom | base64
AUTH_ROOT_KEY_BASE64=
AUTH_CSRF_SECRET=
AUTH_VAULT_KEY_BASE64=
AUTH_VAULT_KEY_VERSION=development-v1
AUTH_VAULT_KEY_RING_JSON=
# Require AAL2 (MFA/passkey step-up) before revealing vault secrets. Defaults to true in production.
# AUTH_VAULT_REVEAL_REQUIRE_STEP_UP=false
# The native outbox worker cannot run the derivation above, so `ddd init` also
# writes 32 random bytes here and the app prefers this value when it is set.
AUTH_OUTBOX_KEY_BASE64=
AUTH_OUTBOX_KEY_VERSION=development-v1
AUTH_OUTBOX_POSTGRES_POOL_SIZE=4
AUTH_OUTBOX_MAIL_BATCH_SIZE=25
AUTH_OUTBOX_RELATIONSHIP_BATCH_SIZE=100
AUTH_OUTBOX_POLL_INTERVAL_MS=500
AUTH_RECOVERY_CODE_PEPPER_BASE64=
AUTH_MFA_ISSUER=Buwiz
# Cloud/local default: capture mail + dev tools. Production forbids both.
AUTH_DEV_TOOLS=true
# Optional: mark the new account verified immediately after register (still
# requires mail-capture + AUTH_DEV_TOOLS + loopback). Prefer the UI "Verify now"
# path unless you need headless setup.
AUTH_DEV_AUTO_VERIFY=false
AUTH_MAIL_TRANSPORT=capture
AUTH_MAIL_HTTP_URL=
AUTH_MAIL_HTTP_TOKEN=
# Real inbox (not the cloud/dev default). AUTH_PUBLIC_BASE_URL must be a URL
# the email client can reach (tunnel/public host) — localhost links will not
# open this VM from a phone or another machine. Never commit AUTH_RESEND_API_KEY.
# AUTH_MAIL_TRANSPORT=resend
# AUTH_RESEND_API_KEY=
# AUTH_RESEND_FROM="Buwiz <auth@your-verified-domain.example>"
AUTH_RESEND_API_KEY=
AUTH_RESEND_FROM="Buwiz <auth@your-verified-domain.example>"
AUTH_SPICEDB_ENABLED=false
AUTH_SPICEDB_CHECK_URL=
AUTH_SPICEDB_WRITE_URL=
AUTH_SPICEDB_TOKEN=
AUTH_SPICEDB_CHECK_TOKEN=
AUTH_SPICEDB_MEMBERSHIP_PERMISSION=member
AUTH_SESSION_TTL_SECONDS=3600
AUTH_ACCESS_TOKEN_TTL_SECONDS=900
AUTH_REFRESH_TOKEN_TTL_SECONDS=2592000
AUTH_COOKIE_SECURE=false

# Dashboard board: Resources & queries (server-proxied). Disabled by default; enable explicitly.
# AUTH_DASHBOARD_HTTP_ENABLED=true
# AUTH_DASHBOARD_HTTP_ALLOW_PRIVATE=false
# AUTH_DASHBOARD_GRPC_ENABLED=false

# Storage backend selection and optional reset targets.
DATABASE_BACKEND=postgres
POSTGRES_URL=
DATABASE_URL=

# Optional providers. Leave disabled until credentials and provider callback URLs are configured.
AUTH_ENABLE_OAUTH=false
AUTH_OAUTH_DEVELOPMENT_CALLBACK_BYPASS=false
AUTH_ENABLE_PASSKEYS=false
# Passkeys require a hostname rpId — browsers reject IP addresses (SecurityError).
# When listen is 127.0.0.1:PORT, Makefile defaults these to localhost and
# http://localhost:PORT. Open that URL in the browser (not http://127.0.0.1:PORT).
# AUTH_PASSKEY_RP_ID=localhost
# AUTH_PASSKEY_ORIGIN=http://localhost:3008
AUTH_PASSKEY_RP_NAME=Buwiz
AUTH_PASSKEY_REQUIRE_USER_VERIFICATION=true
AUTH_PASSKEY_REQUIRE_USER_HANDLE=false
AUTH_PASSKEY_STRICT_BASE64=true
AUTH_PASSKEY_AUTHENTICATOR_ATTACHMENT=any
AUTH_PASSKEY_CHALLENGE_TTL_SECONDS=300

# Google OAuth/OIDC.
AUTH_GOOGLE_ENABLED=false
AUTH_GOOGLE_CLIENT_ID=
AUTH_GOOGLE_CLIENT_SECRET=
AUTH_GOOGLE_ISSUER=https://accounts.google.com
AUTH_GOOGLE_AUTHORIZATION_URL=https://accounts.google.com/o/oauth2/v2/auth
AUTH_GOOGLE_TOKEN_URL=https://oauth2.googleapis.com/token
AUTH_GOOGLE_JWKS_URL=https://www.googleapis.com/oauth2/v3/certs
AUTH_GOOGLE_JWKS_JSON=
AUTH_GOOGLE_USERINFO_URL=https://openidconnect.googleapis.com/v1/userinfo
AUTH_GOOGLE_SCOPES=openid email profile
AUTH_GOOGLE_REDIRECT_URI=http://127.0.0.1:3008/api/auth/oauth/google/callback

# Facebook Login.
AUTH_FACEBOOK_ENABLED=false
AUTH_FACEBOOK_CLIENT_ID=
AUTH_FACEBOOK_CLIENT_SECRET=
AUTH_FACEBOOK_ISSUER=https://www.facebook.com
AUTH_FACEBOOK_AUTHORIZATION_URL=https://www.facebook.com/v20.0/dialog/oauth
AUTH_FACEBOOK_TOKEN_URL=https://graph.facebook.com/v20.0/oauth/access_token
AUTH_FACEBOOK_USERINFO_URL=https://graph.facebook.com/v20.0/me?fields=id,email,name
AUTH_FACEBOOK_SCOPES=email public_profile
AUTH_FACEBOOK_REDIRECT_URI=http://127.0.0.1:3008/api/auth/oauth/facebook/callback

# Apple Login.
AUTH_APPLE_ENABLED=false
AUTH_APPLE_CLIENT_ID=
AUTH_APPLE_GENERATED_CLIENT_SECRET=
AUTH_APPLE_TEAM_ID=
AUTH_APPLE_KEY_ID=
AUTH_APPLE_PRIVATE_KEY=
AUTH_APPLE_CLIENT_SECRET_TTL_SECONDS=86400
AUTH_APPLE_ISSUER=https://appleid.apple.com
AUTH_APPLE_AUTHORIZATION_URL=https://appleid.apple.com/auth/authorize
AUTH_APPLE_TOKEN_URL=https://appleid.apple.com/auth/token
AUTH_APPLE_JWKS_URL=https://appleid.apple.com/auth/keys
AUTH_APPLE_JWKS_JSON=
AUTH_APPLE_SCOPES=openid email name
AUTH_APPLE_REDIRECT_URI=http://127.0.0.1:3008/api/auth/oauth/apple/callback

# Redis is a wake bus only (not the source of truth). Leave empty to skip publishes.
REDIS_URL=redis://127.0.0.1:6379
REDIS_CHANNEL=buwiz-tax-profiles

# Local/dev TIN-ownership proof. Production ORUS is not wired (see docs/extensions.md).
ORUS_FAKE_VERIFIER=false

# Client-side WebMCP on auth forms. Safe when the browser API is missing.
WEBMCP_ENABLED=true

# Extra desktop OAuth redirect URIs (comma-separated). Loopback HTTP(S) and
# buwiz://auth/callback are always allowed.
DESKTOP_OAUTH_REDIRECT_URIS=
